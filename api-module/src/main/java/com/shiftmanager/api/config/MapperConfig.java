package com.shiftmanager.api.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration for object mapping
 * Note: MapStruct is used for mapping between DTOs and entities
 */
@Configuration
public class MapperConfig {
    // This is a placeholder class for future mapper configuration
    // MapStruct mappers are generated at compile time and don't require explicit configuration
}
